name: string_normalizer
description: Helps you to remove accents and diacritics from strings, it supports both static method and extension.
version: 0.2.1
homepage: https://github.com/lamnhan066/string_normalizer

environment:
  sdk: ">=2.18.0 <4.0.0"

dev_dependencies:
  test: ^1.21.3
  lints: ^2.0.0
  http: ^1.1.0
